class RobotExecutor:

    def execute(self, program):
        pass

        # for instruction in program:

            # if isinstance(instruction, MoveJ):
            #
            #     self.execute_movej(instruction)
            #
            # elif isinstance(instruction, MoveL):
            #
            #     self.execute_movel(instruction)